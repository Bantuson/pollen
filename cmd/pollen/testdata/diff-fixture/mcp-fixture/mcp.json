{
  "_comment": "Synthetic MCP config for the differential test fixture. The image ref is deliberately implausible so this fixture cannot collide with a real catalog entry.",
  "mcpServers": {
    "diff-fixture-mcp": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "ghcr.io/diff-fixture/canary-mcp:1.0.0"
      ]
    },
    "diff-fixture-npx": {
      "command": "npx",
      "args": [
        "-y",
        "@diff-fixture/canary-server@2.0.0"
      ]
    }
  }
}
