# -*- encoding: utf-8 -*-
# Cross-platform parity test fixture gemspec (NOT a real gem).

Gem::Specification.new do |s|
  s.name    = "parity-gem"
  s.version = "1.0.0"
  s.summary = "Parity test fixture gem"
end
